# Sometimes we need to convert one data type to another.
# In python this is achieved with type casting
# This is currently a string type even though it has integer value 
a = "45"

# Converting to integer
a = int(a)

# Converting to Float
a = float(a)

print(type(a))