# Set is a collection of non repitivs elements
# Sets are data types in Py which contains uniquie values always
# Sets are unordered and unindexed
a = { 1, 5, 4, 6 ,1}

print(a)

# This create a empty "dict" instead of a "set"
b = {}
print(type(b))

# We create empty set as:
c = set()
print(type(c))